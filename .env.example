# ===== Server =====
PORT=10000
NODE_ENV=production

# Token compartido para endpoints protegidos (x-auth-token)
SHARED_TOKEN=dev-secret

# ===== ForUsAll / Credenciales del portal =====
# Reemplaza con tus credenciales reales en el despliegue (NUNCA las subas al repo).
SITE_USER=your.portal.user
SITE_PASS=your.portal.pass

# Secreto TOTP en Base32 (sin espacios).
TOTP_SECRET=JBSWY3DPEHPK3PXP
TOTP_STEP_SECONDS=30

# ===== Concurrencia y ETA =====
MAX_CONCURRENCY=3
ESTIMATE_AVG_SECONDS=120
ESTIMATE_AVG_WINDOW=10

# ===== Evidencia (screenshots/logs) =====
EVIDENCE_DIR=/tmp/evidence

# ===== Playwright (opcional) =====
# PLAYWRIGHT_BROWSERS_PATH=0
